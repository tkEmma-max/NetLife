import 'package:flutter/material.dart';

class AppColors {
  // Nature & urgence
  static const Color forest = Color(0xFF2D6A4F);    // Vert forêt - signature LifeNet
  static const Color danger = Color(0xFFE63946);    // Rouge - urgence/alerte
  static const Color water = Color(0xFF00B4D8);     // Bleu eau - secondaire

  // Neutres
  static const Color background = Color(0xFFF8F9FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color text = Color(0xFF212529);
  static const Color textLight = Color(0xFF6C757D);

  // États
  static const Color success = Color(0xFF2A9D8F);    // Vert turquoise - résolu
}